<UserControl x:Class="CarRental.UI.Views.DriverRentalsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CarRental.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel  DataContext="{Binding DriverRentalsViewModel, Source={StaticResource Locator}}" Background="White">
        <ListView Style="{DynamicResource MaterialDesignListView}" SelectionMode="Single" ItemsSource="{Binding DriverRentals}" SelectedItem="{Binding SelectedRental}" >
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Registration Number" DisplayMemberBinding="{Binding RegistrationNumber}" />
                    <GridViewColumn Header="Driver Name" DisplayMemberBinding="{Binding DriverName}"/>
                    <GridViewColumn Header="Start Time" DisplayMemberBinding="{Binding StartDateTime}"/>
                    <GridViewColumn Header="Stop Time" DisplayMemberBinding="{Binding StopDateTime}" />
                    <GridViewColumn Header="Total" DisplayMemberBinding="{Binding Total}" />
                </GridView>
            </ListView.View>
        </ListView>
    </StackPanel>
</UserControl>
